<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Theme (Dark Mode sẽ apply tự động theo system nếu bạn dùng DayNight) -->
    <style name="Base.Theme.Android_lab" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Primary Brand Colors -->
        <item name="colorPrimary">@color/primary_dark</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Secondary Colors -->
        <item name="colorSecondary">@color/accent</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Background (Quan trọng: dùng đúng attr không có android:) -->
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="colorSurface">#1E1E1E</item>

        <!-- System UI -->
        <item name="android:statusBarColor">#1A1A1A</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Text Colors -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/text_color_unselected</item>

        <!-- Component Styles -->
        <item name="materialCardViewStyle">@style/Widget.App.CardView.Dark</item>
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigation.Dark</item>
    </style>

    <!-- CardView Style -->
    <style name="Widget.App.CardView.Dark" parent="Widget.Material3.CardView.Elevated">
        <item name="cardCornerRadius">12dp</item>
        <item name="cardElevation">4dp</item>
        <item name="contentPadding">0dp</item>
        <item name="cardBackgroundColor">#2A2A2A</item>
    </style>

    <!-- Bottom Navigation Style -->
    <style name="Widget.App.BottomNavigation.Dark" parent="Widget.Material3.BottomNavigationView">
        <item name="android:background">@drawable/button_background_dark</item>
        <item name="itemIconTint">@color/bottom_nav_colors_dark</item>
        <item name="itemTextColor">@color/bottom_nav_colors_dark</item>
        <item name="elevation">8dp</item>
    </style>

    <!-- Final Theme Entry -->
    <style name="Theme.Android_lab" parent="Base.Theme.Android_lab" />
</resources>
