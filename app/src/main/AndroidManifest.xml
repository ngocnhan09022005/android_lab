<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions cho image pick -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android_lab"
        tools:targetApi="31">

        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Các activity còn lại đều nên khai báo exported=false -->
        <activity android:name=".ui.admin.AdminActivity" android:exported="false" />
        <activity android:name=".ui.user.MainActivity" android:exported="false"/>
        <activity android:name=".ui.user.IntroActivity" android:exported="false" />
        <activity android:name=".ui.auth.LoginActivity" android:exported="false" />
        <activity android:name=".ui.auth.RegisterActivity" android:exported="false" />
        <activity android:name=".ui.user.MenuFoodActivity" android:exported="false" />
        <activity android:name=".ui.user.MenuDrinkActivity" android:exported="false" />
        <activity android:name=".ui.user.FoodDetailActivity" android:exported="false" />
        <activity android:name=".ui.user.DrinkDetailActivity" android:exported="false" />
        <activity android:name=".ui.admin.MenuActivity" android:exported="false" />

    </application>

</manifest>
